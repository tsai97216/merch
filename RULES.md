# merch 開發規則與寫法

> 本文件用來記錄已確立的開發規則、資料結構與 UI/UX 行為。之後新增或修改功能時，先閱讀本文件，避免重複踩坑或讓新功能與既有設計不一致。
>
> 原則：**規則確立後就記錄；規則變更時同步更新本文件。**

## 1. UI/UX 基準

- UI/UX 與功能行為以 `tsai97216/merch-old` 為主要參考基準。
- 新架構重新實作，不直接複製 `merch-old` 程式碼。
- 新增元件或頁面時，優先沿用現有視覺語言、間距、圓角、陰影、字體階層與互動方式。
- 不要為單一頁面任意建立與全站不同的 UI 規則。

## 2. 版本號

- 唯一正式版本來源：`public/data/version.json`。
- 格式：`Major.Minor.Patch`。
- `Major`：大版本／架構世代重寫。
- `Minor`：每次完整邏輯／功能修改。
- `Patch`：GitHub API 新增／移除收藏或圖片。
- reorder 與 cover replacement 不增加 Patch。
- 不使用版本號作為 module cache。
- 不使用 `?v=version`、`?build=...` 等 cache hack。

## 3. Work / 作品

- 作品的「顯示名稱」與「內部代碼」必須分開。
- `Work.name`：使用者看到的完整作品名稱，例如 `崩壞：星穹鐵道`。
- `Work.code`：內部短代碼，例如 `HSR`。
- 不要把 `name` 當作 code 使用，也不要在 UI 顯示 code 取代完整作品名稱，除非該處明確需要內部代碼。
- 目前作品代碼：
  - `HSR` = 崩壞：星穹鐵道
  - `GI` = 原神
  - `ZZZ` = 絕區零
  - `WW` = 鳴潮

## 4. Item ID

- Item ID 永久穩定，刪除後不可重新編號。
- ID 依「作品 + 類型」分組遞增。
- 格式：`作品代碼 + 類型代碼 + 三位流水號`，例如 `GIa001`、`HSRf001`。
- 新增 Item 時使用現有 ID 規則產生下一個序號。
- 不因刪除舊 Item 而填補中間缺號。

## 5. Category / 類型

- 類型有內部 code 與完整顯示名稱，兩者分開保存。
- 目前類型 code：
  - `a` 壓克力／亞克力
  - `b` 徽章／吧唧
  - `c` 卡片／訂金卡
  - `d` 立牌／擺件
  - `e` 電子產品
  - `f` 手辦／模型
  - `g` 文具
  - `h` 海報／掛畫／掛軸
  - `k` 掛件／吊飾
  - `l` 文件／資料夾
  - `m` 書籍／漫畫
  - `n` 明信片
  - `p` 毛絨／布偶
  - `q` 鑰匙圈
  - `r` 雷射票
  - `s` 色紙
  - `v` 服飾
  - `w` 餐具／生活用品
  - `y` 特典
  - `o` 其他
- 新增類型時，同步更新 `public/data/categories.json` 與相關 validation / 顯示邏輯。

## 6. 搜尋與跨頁跳轉

- **任何可以代表搜尋條件的統計、排名或摘要項目，只要使用者點擊後合理地可以查看對應收藏，就應提供搜尋轉跳。**
- 目前已確立的行為：
  - 主頁「角色排名」點角色名稱 → `#/collection`，搜尋該角色。
  - 主頁「作品統計」點作品名稱 → `#/collection`，搜尋該作品。
  - 統計頁「作品」點作品名稱 → `#/collection`，搜尋該作品。
  - 統計頁「類型」點類型名稱 → `#/collection`，搜尋該類型。
- 未來新增：
  - 角色統計／排名 → 點角色 → 搜尋角色。
  - 廠商統計 → 點廠商 → 搜尋廠商。
  - 狀態統計 → 點狀態 → 搜尋對應狀態。
  - 其他可唯一對應收藏的統計 → 原則上都應提供搜尋轉跳。
- 新增這類 UI 時，**不要只完成視覺顯示，要同時實作點擊 → Collection → 搜尋結果的完整流程。**
- 目前跨頁搜尋集中在 `src/cross-navigation.ts`，動態渲染的統計／排名元素可透過事件 delegation 處理。
- 搜尋應使用使用者看到的名稱或可被搜尋系統識別的值，不應把內部 code 當成唯一 UI 搜尋文字。

## 7. Collection 搜尋

- Collection 搜尋狀態由 Store 管理，不讓頁面各自持有一份獨立資料副本。
- 搜尋、Filter、Sort 與顯示模式的狀態要保持一致。
- 從其他頁面跳轉進 Collection 搜尋時，必須讓畫面實際更新為對應搜尋結果，而不是只改 URL。
- 搜尋無結果時維持既有 Empty State。

## 8. Rendering / DOM

- 使用者輸入、API 資料與外部資料一律視為不可信。
- 優先使用安全 DOM rendering 與既有 escape / utility，不要直接把未處理的外部字串塞進 `innerHTML`。
- 新增 DOM 行為時，優先沿用現有 class 與結構，不要任意建立重複元件。
- 動態產生的元素若需要點擊行為，優先使用事件 delegation，避免每次 render 重複綁定 listener。

## 9. Data / Store

- Page 不持有自己的資料副本。
- Store 是資料與 UI state 的主要來源。
- State 更新採 immutable 原則。
- 新增資料欄位時，要同步考慮：type、schema validation、store、rendering、migration 與既有資料相容性。
- 不要只修改 UI 而忘記 data validation。

## 10. 新增功能時的檢查清單

新增一個功能時，至少檢查：

1. 是否需要新增或修改 type？
2. 是否需要 schema validation？
3. 是否需要更新 Store？
4. 是否需要更新 UI rendering？
5. 是否需要搜尋／篩選／排序整合？
6. 如果是統計、排名或摘要，是否應提供點擊搜尋轉跳？
7. 是否需要手機版行為？
8. 是否需要 loading / empty / error 狀態？
9. 是否影響版本號？
10. 是否需要更新 TODO.md？
11. 是否需要更新本 RULES.md，讓下一次修改的人知道這個規則？

## 11. TODO.md

- `TODO.md` 是目前專案的進度追蹤清單。
- 實際完成才勾選 `[x]`。
- 只完成一部分時維持 `[ ]`，可在備註補充進度。
- 完成功能後應同步檢查是否有對應 TODO 項目需要更新。

## 12. 規則維護

- 本文件不是一次性說明，而是持續累積的專案規格。
- 發現容易重複出錯的寫法、確立新的資料規則、UI 行為或跨頁互動時，應把規則補進本文件。
- 如果新需求與既有規則衝突，先確認需求，再修改本文件與程式，避免只改程式而留下過時規則。
