name: Apply worker shipping patch
on:
  push:
    branches: [main]
    paths:
      - 'scripts/patch-worker-shipping.py'
      - '.github/workflows/apply-worker-shipping.yml'
permissions:
  contents: write
jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[skip shipping]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/patch-worker-shipping.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -f scripts/patch-worker-shipping.py .github/workflows/apply-worker-shipping.yml
          git add .
          git commit -m 'fix: add worker shipping endpoints [skip shipping]'
          git push
