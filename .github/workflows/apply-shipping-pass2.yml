name: Apply shipping pass 2
on:
  push:
    branches: [main]
    paths:
      - 'scripts/fix-shipping-pass2.py'
      - '.github/workflows/apply-shipping-pass2.yml'
permissions:
  contents: write
jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[skip shipping]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/fix-shipping-pass2.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -f scripts/fix-shipping-pass2.py .github/workflows/apply-shipping-pass2.yml
          git add .
          git commit -m 'fix: complete shipping integration [skip shipping]'
          git push
