{
  "name": "chi-merch",
  "private": true,
  "version": "1.109.642",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/sync-public-data.mjs && node scripts/generate-collection.mjs && tsc --noEmit && vite build",
