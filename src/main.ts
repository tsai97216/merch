import './styles.css';
import './item-detail-modal.css';
import './collection.css';
import { createRouter } from './router';
import { loadStore, MerchStore } from './store';
import { toAppError } from './error';
import { parseItemId } from './item-id';
import { resolveAssetUrl } from './image-source';
import { parseDate } from './utils/date';
import { showToast } from './utils/toast';
import { categoryName } from './category-label';
import type { Item } from './types';

const $ = <T extends Element>(selector: string, root: ParentNode = document) => root.querySelector<T>(selector);
const $$ = <T extends Element>(selector: string, root: ParentNode = document) => [...root.querySelectorAll<T>(selector)];
const escapeHtml = (value: unknown) => String(value ?? '').replace(/[&<>\\"']/g, (c) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c] as string));
const money = (n?: number) => n == null ? '—' : `NT$ ${new Intl.NumberFormat('zh-TW').format(Number(n))}`;
const statusText = (s: string) => ({ received:'已收到', preorder:'預購中', pending:'待到貨' } as Record<string,string>)[s] || s || '未設定';
const dateText = (s?: string | null) => { const date = parseDate(s || undefined); return date ? date.toLocaleDateString('zh-TW') : '—'; };
const quantityOf = (item: Item) => Number.isInteger(item.quantity) && item.quantity > 0 ? item.quantity : 1;
const itemValue = (item: Item) => Number(item.purchase?.price || 0) * quantityOf(item);
const itemDate = (item: Item) => item.purchase?.date || item.arrival?.receivedDate || '';
function getItemIdParts(item: Item) { return parseItemId(item.id); }
function itemSearchText(item: Item): string { const parsed = getItemIdParts(item); const idParts = parsed ? [parsed.workCode, parsed.categoryCode, `${parsed.workCode}${parsed.categoryCode}`, String(parsed.sequence).padStart(3, '0')] : []; return [item.id, ...idParts, item.title, item.workName, ...(item.series || []), item.category, categoryName(item.category), item.manufacturer, ...(item.characters || []), item.notes, item.description, item.purchase?.platform, statusText(item.status), item.status].filter(Boolean).join(' ').toLowerCase(); }
function splitSearchQuery(query: string): string[] { return query.trim().toLowerCase().split(/[\s,，、]+/).map((term) => term.trim()).filter(Boolean); }
function matchesItemQuery(item: Item, query: string): boolean { const terms = splitSearchQuery(query); if (!terms.length) return true; const searchText = itemSearchText(item); const parsed = getItemIdParts(item); if (terms.length === 1 && parsed) { const normalized = terms[0]; const exactId = item.id.toLowerCase() === normalized; const groupKey = `${parsed.workCode}${parsed.categoryCode}`.toLowerCase(); const workKey = parsed.workCode.toLowerCase(); if (exactId || groupKey === normalized || workKey === normalized) return true; } return terms.every((term) => { if (term === '待到貨') return item.status === 'pending' || item.status === 'preorder'; if (term === '預購中') return item.status === 'preorder'; if (term === '已收到') return item.status === 'received'; return searchText.includes(term); }); }
function itemImageSource(item: Item, image?: Item['images'][number]): string { if (!image?.file) return ''; return resolveAssetUrl(`data/${item.workId}/${item.category}/${item.id}/images/${image.file}`); }
function itemCard(item: Item, compact = false): string { const image = item.images?.find((i) => i.isCover) || item.images?.[0]; const src = itemImageSource(item, image); const quantity = quantityOf(item); const unitPrice = item.purchase?.price; const expected = item.arrival?.expectedDate; const statusBadgeClass = item.status === 'received' ? 'badge is-hidden' : 'badge'; const priceText = `${money(unitPrice)}${quantity > 1 ? ` <small class="quantity-mark">×${quantity}</small>` : ''}`; return `<article class="item-card ${compact ? 'compact':''}" data-item-id="${escapeHtml(item.id)}" tabindex="0" role="button" aria-label="查看 ${escapeHtml(item.title)} 詳情"><div class="item-media">${src ? `<img src="${escapeHtml(src)}" alt="${escapeHtml(image?.alt || item.title)}" loading="lazy" onerror="this.style.display='none';this.parentElement.classList.add('image-fallback')">` : '<span class="image-placeholder">NO IMAGE</span>'}</div><div class="item-body"><div class="item-top"><span class="muted">${escapeHtml(categoryName(item.category))}</span><span class="${statusBadgeClass}">${escapeHtml(statusText(item.status))}</span></div><h3>${escapeHtml(item.title)}</h3><p class="item-work">${escapeHtml(item.workName || '')}${item.characters?.length ? ` · ${escapeHtml(item.characters.join('、'))}` : ''}</p><div class="item-bottom"><strong>${priceText}</strong><span>${escapeHtml(item.manufacturer || '')}</span></div>${item.status !== 'received' && expected ? `<small class="expected-arrival">預計到貨 ${escapeHtml(dateText(expected))}</small>` : ''}</div></article>`; }
function openItemCard(card: Element): void { const id = card.getAttribute('data-item-id'); if (id) location.hash = `#/item/${encodeURIComponent(id)}`; }
function setupItemCardDelegation(): void { const root = document.querySelector<HTMLElement>('#app') || document.body; if (root.dataset.itemCardDelegationReady === 'true') return; root.dataset.itemCardDelegationReady = 'true'; root.addEventListener('click', (event) => { const target = event.target as Element | null; const card = target?.closest<HTMLElement>('.item-card'); if (card && root.contains(card)) openItemCard(card); }); root.addEventListener('keydown', (event) => { const keyEvent = event as KeyboardEvent; if (keyEvent.key !== 'Enter' && keyEvent.key !== ' ') return; const target = event.target as Element | null; const card = target?.closest<HTMLElement>('.item-card'); if (!card || !root.contains(card)) return; keyEvent.preventDefault(); openItemCard(card); }); }
function optionList(items: Item[], valueOf: (item: Item) => string | string[] | undefined): string[] { return [...new Set(items.flatMap((item) => { const value = valueOf(item); if (Array.isArray(value)) return value.filter(Boolean); return value ? [value] : []; }))].sort((a, b) => a.localeCompare(b, 'zh-Hant')); }
function setSelectOptions(id: string, allLabel: string, values: string[], selected: string) { const select = $<HTMLSelectElement>(`#${id}`); if (!select) return; select.innerHTML = [`<option value="all">${escapeHtml(allLabel)}</option>`, ...values.map((value) => `<option value="${escapeHtml(value)}">${escapeHtml(value)}</option>`)].join(''); select.value = values.includes(selected) ? selected : 'all'; }
function syncDynamicCollectionOptions(store: MerchStore) { const { items, ui } = store.snapshot; setSelectOptions('filter-category', '全部類型', optionList(items, (item) => categoryName(item.category)), ui.collectionCategory); setSelectOptions('filter-character', '全部角色', optionList(items, (item) => item.characters), ui.collectionCharacter); setSelectOptions('filter-manufacturer', '全部廠商', optionList(items, (item) => item.manufacturer), ui.collectionManufacturer); }
function renderHome(store: MerchStore) { const { items, works } = store.snapshot; const total = items.reduce((sum, item) => sum + quantityOf(item), 0); const pending = items.filter((i) => i.status === 'pending' || i.status === 'preorder').reduce((sum, item) => sum + quantityOf(item), 0); const spending = items.reduce((sum, i) => sum + itemValue(i), 0); const now = new Date(); const month = items.filter((i) => { const d = parseDate(i.purchase?.date); return !!d && d.getFullYear() === now.getFullYear() && d.getMonth() === now.getMonth(); }).reduce((s, i) => s + itemValue(i), 0); const set = (key: string, value: string) => { const e = $<HTMLElement>(`[data-home="${key}"]`); if (e) e.textContent = value; }; set('total', String(total)); set('pending', String(pending)); set('month', money(month)); set('spending', money(spending)); const bars = $('#work-bars'); if (bars) { const rows = works.map((w) => ({ work: w, spend: w.items.reduce((sum, item) => sum + itemValue(item), 0) })).sort((a, b) => b.spend - a.spend || a.work.name.localeCompare(b.work.name, 'zh-Hant')); const maxSpend = rows[0]?.spend || 0; bars.innerHTML = rows.map(({ work, spend }) => `<div class="bar-row" data-search-query="${escapeHtml(work.name)}"><span>${escapeHtml(work.name)}</span><div><i style="width:${maxSpend ? Math.max(4, spend / maxSpend * 100) : 4}%"></i></div><b>${escapeHtml(money(spend))}</b></div>`).join('') || '<div class="empty-state">目前沒有資料</div>'; } const recent = $('#recent-items'); if (recent) recent.innerHTML = [...items].sort((a, b) => String(itemDate(b)).localeCompare(String(itemDate(a)))).slice(0, 4).map((i) => itemCard(i, true)).join('') || '<div class="empty-state">目前沒有收藏</div>'; const rank = $('#character-ranking'); const counts = new Map<string, number>(); items.forEach((i) => (i.characters || []).forEach((c) => counts.set(c, (counts.get(c) || 0) + quantityOf(i)))); const top = [...counts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 5); if (rank) rank.innerHTML = top.length ? top.map(([c, n], idx) => `<li><span>${idx + 1}</span><strong data-search-query="${escapeHtml(c)}">${escapeHtml(c)}</strong><b>${n}</b></li>`).join('') : '<li class="empty-state">目前沒有資料</li>'; }
function renderCollection(store: MerchStore) { const host = $('#collection-items'); if (!host) return; const { items, ui } = store.snapshot; const query = ui.collectionQuery.trim().toLowerCase(); const workCode = ui.collectionWork === 'all' ? 'all' : (store.snapshot.works.find((w) => w.id === ui.collectionWork)?.code || ui.collectionWork); let filtered = items.filter((i) => matchesItemQuery(i, query)).filter((i) => ui.collectionStatus === 'all' || (ui.collectionStatus === 'pending' ? i.status === 'pending' || i.status === 'preorder' : i.status === ui.collectionStatus)).filter((i) => workCode === 'all' || getItemIdParts(i)?.workCode === workCode).filter((i) => ui.collectionCategory === 'all' || categoryName(i.category) === ui.collectionCategory).filter((i) => ui.collectionCharacter === 'all' || (i.characters || []).includes(ui.collectionCharacter)).filter((i) => ui.collectionManufacturer === 'all' || i.manufacturer === ui.collectionManufacturer); filtered = [...filtered].sort((a, b) => ui.collectionSort === 'price' ? Number(b.purchase?.price || 0) - Number(a.purchase?.price || 0) : ui.collectionSort === 'title' ? a.title.localeCompare(b.title, 'zh-Hant') : String(itemDate(b)).localeCompare(String(itemDate(a)))); host.innerHTML = filtered.map((i) => itemCard(i)).join('') || '<div class="empty-state wide">沒有符合條件的收藏</div>'; host.classList.toggle('list-mode', ui.collectionView === 'list'); const count = $('#collection-count'); if (count) count.textContent = `${filtered.reduce((sum, item) => sum + quantityOf(item), 0)} ITEMS`; $$('.view-button').forEach((button) => { const active = button.dataset.view === ui.collectionView; button.classList.toggle('is-active', active); button.setAttribute('aria-pressed', String(active)); }); }
function renderStatistics(store: MerchStore) { const { items } = store.snapshot; const total = items.reduce((sum, item) => sum + quantityOf(item), 0); const pending = items.filter((i) => i.status !== 'received').reduce((sum, item) => sum + quantityOf(item), 0); const spending = items.reduce((s, i) => s + itemValue(i), 0); const set = (k: string, v: string) => { const e = $<HTMLElement>(`[data-stat="${k}"]`); if (e) e.textContent = v; }; set('total', String(total)); set('pending', String(pending)); set('spending', money(spending)); }
let detailModal: HTMLElement | null = null; let detailUnderlyingRoute = 'collection'; let appStore: MerchStore | null = null;
function createDetailDialogHtml(): string { return `<button type="button" class="item-detail-close" aria-label="關閉" data-detail-close><i class="fa-solid fa-xmark"></i></button><div class="item-detail-header"><div class="item-detail-image" id="item-detail-image">IMAGE</div><div class="item-detail-heading"><span class="eyebrow">ITEM DETAIL</span><h2 id="item-detail-title"></h2><p id="item-detail-subtitle"></p><div id="item-detail-status"></div></div></div><div class="item-detail-grid"><section><span class="detail-label">基本資訊</span><dl><div><dt>作品</dt><dd id="detail-work"></dd></div><div><dt>系列</dt><dd id="detail-series"></dd></div><div><dt>角色</dt><dd id="detail-character"></dd></div><div><dt>類型</dt><dd id="detail-type"></dd></div><div><dt>製造商</dt><dd id="detail-manufacturer"></dd></div></dl></section><section><span class="detail-label">購買資訊</span><dl><div><dt>單價</dt><dd id="detail-price"></dd></div><div><dt>數量</dt><dd id="detail-quantity"></dd></div><div><dt>合計</dt><dd id="detail-total-price"></dd></div><div><dt>幣別</dt><dd id="detail-currency"></dd></div><div><dt>平台</dt><dd id="detail-platform"></dd></div><div><dt>購買日期</dt><dd id="detail-purchase-date"></dd></div></dl></section><section><span class="detail-label">到貨資訊</span><dl><div><dt>預計到貨</dt><dd id="detail-expected-date"></dd></div><div><dt>收到日期</dt><dd id="detail-received-date"></dd></div></dl></section><section><span class="detail-label">售後</span><dl><div><dt>狀態</dt><dd id="detail-after-sales-status"></dd></div><div><dt>備註</dt><dd id="detail-after-sales-note"></dd></div></dl></section></div><section class="item-detail-text"><div><span class="detail-label">商品描述</span><p id="detail-description"></p></div><div><span class="detail-label">備註</span><p id="detail-notes"></p></div></section><div class="item-detail-footer"><div class="item-detail-actions"><button type="button" class="button" data-detail-edit>編輯</button><button type="button" class="button danger" data-detail-delete>刪除</button></div></div>`; }
function ensureDetailModal() { if (detailModal) return detailModal; const modal = document.createElement('div'); modal.className = 'item-detail-modal'; modal.hidden = true; modal.innerHTML = `<div class="item-detail-backdrop" data-detail-close></div><section class="item-detail-dialog" role="dialog" aria-modal="true" aria-labelledby="item-detail-title" data-detail-item-id="">${createDetailDialogHtml()}</section>`; document.body.appendChild(modal); modal.addEventListener('click', async (event) => { const target = event.target as Element | null; if (target?.closest('[data-detail-close]')) { closeDetailModal(); return; } const dialog = $('.item-detail-dialog', modal); const id = dialog?.getAttribute('data-detail-item-id') || ''; if (target?.closest('[data-detail-edit]')) { if (id) { sessionStorage.setItem('merch-management-selected-id', id); location.hash = '#/management'; } return; } if (target?.closest('[data-detail-delete]')) { const item = appStore?.snapshot.items.find((entry) => entry.id === id); const button = target.closest<HTMLButtonElement>('[data-detail-delete]'); if (!item || !appStore || !button) { showToast('找不到目前收藏。', 'error'); return; } if (!window.confirm(`確定要刪除「${item.title}」？`)) return; button.disabled = true; const originalText = button.textContent || '刪除'; button.textContent = '刪除中…'; try { await appStore.deleteItem(item.id); showToast('收藏已刪除。', 'success'); closeDetailModal(); } catch (error) { button.disabled = false; button.textContent = originalText; showToast(error instanceof Error ? error.message : '刪除收藏失敗。', 'error'); } } }); document.addEventListener('keydown', (event) => { if (event.key === 'Escape' && detailModal && !detailModal.hidden) closeDetailModal(); }); detailModal = modal; return modal; }
function setDetailText(modal: HTMLElement, id: string, value: unknown) { const node = modal.querySelector<HTMLElement>(`#${id}`); if (node) node.textContent = value == null || value === '' ? '—' : String(value); }
function closeDetailModal() { if (detailModal) { detailModal.hidden = true; document.body.classList.remove('detail-modal-open'); } const target = `#/${detailUnderlyingRoute}`; if (location.hash !== target) location.hash = target; }
function renderDetail(store: MerchStore, id: string) { const modal = ensureDetailModal(); const dialog = $('.item-detail-dialog', modal); if (!dialog) return; const item = store.snapshot.items.find((i) => i.id === id); if (!item) { dialog.setAttribute('data-detail-item-id', ''); dialog.innerHTML = `<div class="item-detail-error"><button type="button" class="item-detail-close" aria-label="關閉" data-detail-close><i class="fa-solid fa-xmark"></i></button><span class="eyebrow">404 / ITEM</span><h2>找不到收藏</h2><p>這個收藏不存在或已被移除。</p></div>`; modal.hidden = false; document.body.classList.add('detail-modal-open'); return; } if (!dialog.querySelector('#item-detail-title')) dialog.innerHTML = createDetailDialogHtml(); dialog.setAttribute('data-detail-item-id', item.id); const characters = item.characters?.join('、') || '—'; const series = item.series?.join('、') || '—'; const afterSalesStatus = item.afterSales?.status; const afterSalesNote = item.afterSales?.note; setDetailText(modal, 'item-detail-title', item.title || '收藏詳細資訊'); setDetailText(modal, 'item-detail-subtitle', `${item.workName || '—'} · ${characters}`); setDetailText(modal, 'detail-work', item.workName); setDetailText(modal, 'detail-series', series); setDetailText(modal, 'detail-character', characters); setDetailText(modal, 'detail-type', categoryName(item.category)); setDetailText(modal, 'detail-manufacturer', item.manufacturer); setDetailText(modal, 'detail-price', money(item.purchase?.price)); setDetailText(modal, 'detail-quantity', quantityOf(item)); setDetailText(modal, 'detail-total-price', item.purchase?.price == null ? undefined : money(itemValue(item))); setDetailText(modal, 'detail-currency', item.purchase?.currency); setDetailText(modal, 'detail-platform', item.purchase?.platform); setDetailText(modal, 'detail-purchase-date', dateText(item.purchase?.date)); setDetailText(modal, 'detail-expected-date', dateText(item.arrival?.expectedDate)); setDetailText(modal, 'detail-received-date', dateText(item.arrival?.receivedDate)); setDetailText(modal, 'detail-after-sales-status', afterSalesStatus); setDetailText(modal, 'detail-after-sales-note', afterSalesNote); setDetailText(modal, 'detail-description', item.description); setDetailText(modal, 'detail-notes', item.notes); const status = $('#item-detail-status', modal); if (status) status.innerHTML = item.status === 'received' ? '<span class="badge is-hidden" aria-hidden="true"></span>' : `<span class="badge">${escapeHtml(statusText(item.status))}</span>`; const image = $('#item-detail-image', modal); const cover = item.images?.find((entry) => entry.isCover) || item.images?.[0]; const imageSrc = itemImageSource(item, cover); if (image) image.innerHTML = imageSrc ? `<img src="${escapeHtml(imageSrc)}" alt="${escapeHtml(cover?.alt || item.title || '')}" onerror="this.style.display='none';this.parentElement.classList.add('image-fallback')">` : '<span class="image-placeholder">IMAGE</span>'; modal.hidden = false; document.body.classList.add('detail-modal-open'); }
function syncCollectionControls(store: MerchStore) { const { ui } = store.snapshot; const search = $('#collection-search') as HTMLInputElement | null; const work = $('#filter-work') as HTMLSelectElement | null; const status = $('#filter-status') as HTMLSelectElement | null; const category = $('#filter-category') as HTMLSelectElement | null; const character = $('#filter-character') as HTMLSelectElement | null; const manufacturer = $('#filter-manufacturer') as HTMLSelectElement | null; const sort = $('#sort') as HTMLSelectElement | null; if (search) search.value = ui.collectionQuery; if (work) work.value = ui.collectionWork; if (status) status.value = ui.collectionStatus === 'preorder' ? 'pending' : ui.collectionStatus; if (category) category.value = ui.collectionCategory; if (character) character.value = ui.collectionCharacter; if (manufacturer) manufacturer.value = ui.collectionManufacturer; if (sort) sort.value = ui.collectionSort; }
function setupCollection(store: MerchStore) { const search = $('#collection-search') as HTMLInputElement | null; const work = $('#filter-work') as HTMLSelectElement | null; const status = $('#filter-status') as HTMLSelectElement | null; const category = $('#filter-category') as HTMLSelectElement | null; const character = $('#filter-character') as HTMLSelectElement | null; const manufacturer = $('#filter-manufacturer') as HTMLSelectElement | null; const sort = $('#sort') as HTMLSelectElement | null; search?.addEventListener('input', () => store.setUi({ collectionQuery: search.value })); work?.addEventListener('change', () => store.setUi({ collectionWork: work.value })); status?.addEventListener('change', () => store.setUi({ collectionStatus: status.value === 'pending' ? 'pending' : status.value })); category?.addEventListener('change', () => store.setUi({ collectionCategory: category.value })); character?.addEventListener('change', () => store.setUi({ collectionCharacter: character.value })); manufacturer?.addEventListener('change', () => store.setUi({ collectionManufacturer: manufacturer.value })); sort?.addEventListener('change', () => store.setUi({ collectionSort: sort.value as 'created' | 'title' | 'price' })); $$('.view-button').forEach((button) => button.addEventListener('click', () => store.setUi({ collectionView: button.dataset.view === 'list' ? 'list' : 'cards' }))); }
function setupSidebarToggle() { const sidebar = $('.sidebar') as HTMLElement | null; const button = $('#sidebar-toggle') as HTMLButtonElement | null; if (!sidebar || !button) return; const key = 'chi-merch-sidebar-collapsed'; const setCollapsed = (collapsed: boolean) => { sidebar.classList.toggle('is-collapsed', collapsed); document.body.classList.toggle('sidebar-collapsed', collapsed); button.setAttribute('aria-label', collapsed ? '展開側邊欄' : '收合側邊欄'); button.title = collapsed ? '展開側邊欄' : '收合側邊欄'; button.innerHTML = '<i class="fa-solid fa-grip-lines-vertical" aria-hidden="true"></i>'; try { localStorage.setItem(key, collapsed ? '1' : '0'); } catch {} }; let collapsed = false; try { collapsed = localStorage.getItem(key) === '1'; } catch {} setCollapsed(collapsed); button.addEventListener('click', () => setCollapsed(!sidebar.classList.contains('is-collapsed'))); }
function renderGlobalError(error: unknown): void { const appError = toAppError(error, '資料載入失敗'); const e = $('#global-error'); if (!e) return; e.hidden = false; e.innerHTML = `<strong>資料載入失敗</strong><p>${escapeHtml(appError.message)}</p><button type="button" id="global-error-retry">重新載入</button>`; }
async function boot() { setupSidebarToggle(); setupItemCardDelegation(); let visiblePage = 'home'; let storeRef: MerchStore | null = null; if (location.hash.startsWith('#/item/')) visiblePage = 'collection'; const router = createRouter({ onNavigate: (route) => { if (route.name === 'item') { detailUnderlyingRoute = visiblePage || 'collection'; if (storeRef) renderDetail(storeRef, route.id); return; } if (route.name === 'not-found') { $$('.page').forEach((page) => { const active = page.getAttribute('data-page') === '404'; page.toggleAttribute('hidden', !active); page.classList.toggle('is-active', active); }); if (detailModal) { detailModal.hidden = true; document.body.classList.remove('detail-modal-open'); } return; } visiblePage = route.name; if (detailModal) { detailModal.hidden = true; document.body.classList.remove('detail-modal-open'); } $$('.page').forEach((page) => { const active = page.getAttribute('data-page') === route.name; page.toggleAttribute('hidden', !active); page.classList.toggle('is-active', active); }); $$('.nav a').forEach((a) => a.classList.toggle('is-active', a.dataset.route === route.name)); const recentSection = $('#recent-section'); if (recentSection) recentSection.toggleAttribute('hidden', route.name !== 'home'); }}); try { const store = await loadStore(); storeRef = store; appStore = store; document.documentElement.dataset.dataReady = 'true'; syncDynamicCollectionOptions(store); syncCollectionControls(store); renderHome(store); renderCollection(store); renderStatistics(store); setupCollection(store); store.subscribe(() => { syncDynamicCollectionOptions(store); syncCollectionControls(store); renderHome(store); renderCollection(store); renderStatistics(store); const match = location.hash.match(/^#\/item\/([^/]+)$/); if (match) renderDetail(store, decodeURIComponent(match[1])); }); router.start(); } catch (error) { console.error(error); renderGlobalError(error); } }
boot();
